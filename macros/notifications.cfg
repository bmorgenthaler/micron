[gcode_macro NOTIFY]
gcode:
    {% set title = params.TITLE|default("🖨️ Micron")|string %}
    {% set msg = params.MSG|default("Notification")|string %}
    {action_call_remote_method("notify",
                               name="gcode_pushover",
                               title=title,
                               message=msg)}